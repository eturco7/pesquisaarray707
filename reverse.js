//O método de array reverse inverte a ordem dos elementos de um array in-place e retorna o array invertido.

const thisarray = [1, 2, 3, 4, 5];

thisarray.reverse();
console.log(thisarray); // Saída: [5, 4, 3, 2, 1]