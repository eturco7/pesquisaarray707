// O every testa se todos os elementos do array passam no teste implementado pela função fornecida.

const umarray = [1, 2, 3, 4, 5];

const MaioresQueZero = umarray.every(elemento => elemento > 0);
console.log(MaioresQueZero); 